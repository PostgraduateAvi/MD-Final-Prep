# Samples for buy_auditor.py

## Example CLI Commands

```
python buy_auditor.py --input samples/products.json --usecase samples/usecase.json --weights samples/weights.json --top 3 --output reports
```

## Expected Outputs

- `reports/audit_report.md`
- `reports/audit_report.json`
- `reports/scores.csv`

See `sample_audit_snippet.md` for a one-product excerpt of the Markdown report.
